const { modelTemplate } = require('../models');

module.exports = {
    // ! Role du controller:
// ? 1 - Récupérer les informations de la requête
// ? 2 - Utiliser les models pour recuperer les données brutes
// ? 3 - Utiliser des services/couches metier pour manipuler les données
// ? 4 - Renvoyer la reponse En JSON
};
